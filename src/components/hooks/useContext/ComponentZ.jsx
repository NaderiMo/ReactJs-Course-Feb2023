import { useContext } from "react";
import ComponentA from "./ComponentA";

export default function ComponentZ()
{
    // const [user]=useContext(DataContext)
   return (<>Z
   {user}
   <button onClick={() => setUser("shohreh")}/>
   </>) 
}